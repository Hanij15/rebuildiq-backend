import { Router } from 'express';
import db from '../db/index.js';
import { authMiddleware } from '../middleware/auth.js';
const router = Router({ mergeParams: true });
router.use(authMiddleware as any);
router.get('/', (req: any, res) => { const est = db.prepare('SELECT * FROM repair_estimates WHERE vehicle_id = ?').get(req.params.id); if (!est) { res.status(404).json({ error: 'No estimate' }); return; } res.json({ ...est, mechanical_labor_hours: est.mech_labor_hours, mechanical_labor_rate: est.mech_labor_rate, adas_calibration_cost: est.adas_calibration, use_override: est.use_total_override }); });
router.put('/', (req: any, res) => { const fields = ['use_detailed','use_total_override','total_override','body_labor_hours','body_labor_rate','paint_labor_hours','paint_labor_rate','mech_labor_hours','mech_labor_rate','frame_labor_hours','frame_labor_rate','paint_materials','shop_supplies','alignment_cost','adas_calibration','misc_cost','transport_cost','other_cost']; const sets: string[] = []; const vals: any[] = []; for (const f of fields) if (req.body[f] !== undefined) { sets.push(`${f} = ?`); vals.push(req.body[f]); } const aliases: Record<string, string> = { mechanical_labor_hours: 'mech_labor_hours', mechanical_labor_rate: 'mech_labor_rate', adas_calibration_cost: 'adas_calibration', use_override: 'use_total_override' }; for (const [frontendName, backendName] of Object.entries(aliases)) { if (req.body[frontendName] !== undefined) { sets.push(`${backendName} = ?`); vals.push(req.body[frontendName]); } } if (sets.length === 0) { res.status(400).json({ error: 'No fields' }); return; } vals.push(req.params.id); db.prepare(`UPDATE repair_estimates SET ${sets.join(', ')} WHERE vehicle_id = ?`).run(...vals); res.json({ success: true }); });
export default router;
